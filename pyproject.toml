[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "genai-competitive-insights"
version = "0.1.0"
description = "RAG-powered competitive intelligence system"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["src*"]
